using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectGen : MonoBehaviour
{

    // Start is called before the first frame update
    void Awake()
    {
        //starting multi-thread task
    }

    // Update is called once per frame
    void Update()
    {

    }


}